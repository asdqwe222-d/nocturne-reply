# 📤 Push Dockerfile - Kommandon

## Kör dessa kommandon i din terminal:

```bash
cd C:\Users\Oliwer\Desktop\Cursor\Nocturne\gpt-relay-server

# Lägg till Dockerfile
git add Dockerfile

# Commit
git commit -m "Fix COPY paths - files are in repo root"

# Push
git push
```

## Efter Push

1. RunPod kommer automatiskt detektera ändringen
2. En ny build startar automatiskt
3. Build bör nu lyckas eftersom filerna finns i root

---

**Kör kommandona ovan för att pusha!** 🚀

